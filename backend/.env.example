# MongoDB Configuration
# For local development
MONGODB_URI=mongodb://localhost:27017/laboratorioCelular
# For MongoDB Atlas (cloud): mongodb+srv://<username>:<password>@<cluster>.mongodb.net/laboratorioCelular
# For Docker: mongodb://mongo:27017/laboratorioCelular

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration (frontend URL that can access this backend)
# Development
FRONTEND_URL=http://localhost:3000
# Production example: https://your-frontend-domain.com

# Security Configuration (required for authentication)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production