# MongoDB Configuration for Docker Compose
# This assumes a MongoDB service named 'mongo' in docker-compose.yml
MONGODB_URI=mongodb://mongo:27017/laboratorioCelular

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration (frontend URL that can access this backend)
# Docker frontend service URL
FRONTEND_URL=http://localhost:3000

# Security Configuration (optional)
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production